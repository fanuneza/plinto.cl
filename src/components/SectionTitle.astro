---
interface Props {
  title: string;
  intro?: string;
}

const { title, intro } = Astro.props;
---

<section class="section-title" data-reveal>
  <h1>{title}</h1>
  {intro && <p>{intro}</p>}
</section>
